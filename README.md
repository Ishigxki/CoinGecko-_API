## Features

- Real-time crypto price ingestion
- Automated data pipeline (runs every 60s)
- PostgreSQL storage with timestamps
- REST API:
  - GET /latest → latest prices
  - GET /history/{coin} → historical data